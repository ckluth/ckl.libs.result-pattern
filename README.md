# ckl.libs.result-pattern

NuGet package providing a Result-pattern based error handling model, imported
step by step from `JOSYN.Foundation.ResultPattern`.

→ [Package documentation](Ckl.Libs.ResultPattern/README.md)

This is the **first** `ckl.libs.<name>` repo — see
[`ckl-builder`](https://github.com/ckluth/ckl-builder) for the ecosystem's
repo-family conventions and the decision-trail this import follows.
